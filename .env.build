# These settings optimize the build process

# Disable telemetry
NEXT_TELEMETRY_DISABLED=1

# Optimize memory usage during build
NODE_OPTIONS=--max-old-space-size=4096

# Skip Prisma generation during build (already done in Docker)
SKIP_ENV_VALIDATION=true

# Production mode
NODE_ENV=production
